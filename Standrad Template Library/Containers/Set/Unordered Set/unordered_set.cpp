#include <bits/stdc++.h>

using namespace std;

void explainUnorderedSet();

int main()
{
explainUnorderedSet();
    return 0;
}

void explainUnorderedSet()
{
// It does not store in a particular order
    unordered_set<int> ust;
// rest is same as set
// lower_bound & upper_bound function does not work
// it has the beter time complexity then set in most cases. Except when collision happens

}