#include <bits/stdc++.h>

using namespace std;

void explainMultiMap();

int main()
{
explainMultiMap();
    return 0;
}

void explainMultiMap()
{
    // it can store duplicated keys
    // it does not include map[key]
    // rest is same as map
}